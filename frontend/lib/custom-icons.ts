// Put your images in frontend/public/icons/custom/, then add their URL here.
// The key matches data-icon on the placeholder (inspect it in the browser).
// Missing entries deliberately render an empty placeholder, with no image request.
export const customIconSources: Readonly<Partial<Record<string, string>>> = {
  // hotel: "/icons/custom/hotel.svg",
  // "booking-calendar": "/icons/custom/booking-calendar.svg",
  // restaurant: "/icons/custom/restaurant.svg",
};
